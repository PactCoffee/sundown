{{#if id}}
  <a href="{{setting 'api_url'}}/admin/users/{{id}}" target="_blank">
   {{first_name}} {{last_name}}
  </a>
{{else}}
  {{#if error}}
    <p>Bad request</p>
  {{else}}
    <p>Sorry, we couldn't find a user with this email</p>
    {{/if}}
{{/if}}
